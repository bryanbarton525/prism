---
name: frontend-vanilla-spa
description: Build minimal single-page UI structure in plain HTML and CSS. Trigger on prompts like "single page app", "vanilla HTML/CSS UI", "no framework frontend".
compatibility: Requires feature requirements and browser-only constraints.
metadata:
  prism-agents: frontend-builder
---

# Vanilla SPA structure

1. Define semantic HTML layout and accessible controls.
2. Keep CSS minimal and readable (layout, spacing, states).
3. Include empty/list/complete visual states.
4. Avoid framework conventions and external dependencies.
5. Return compact JSON summary/findings plus code snippets.
