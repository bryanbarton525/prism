Create the HTML + CSS structure for a minimal todo app.

Requirements:
- semantic markup
- accessible label/button usage
- empty-state rendering hook
- completed-item styling hook

{{EVIDENCE}}

Return JSON with summary, findings, and code snippets.
