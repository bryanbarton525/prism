# Package bootstrap — orchestrator brief

You are adding `internal/metrics` to a Go service. You need a small helper to parse
Prometheus-style label strings before wiring the main `Observe` function.

Deliverable: confirm the helper approach and integrate into the package plan.
